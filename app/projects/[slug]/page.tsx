// Store an array of project objects with slug, title, description, etc.
// In /projects/page.tsx, list the projects
// In /projects/[slug]/page.tsx, use generateStaticParams to statically build each project detail page